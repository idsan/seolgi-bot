export abstract class SeolgiTVConstants {
  static readonly MAX_RETRY_COUNT = 3;
  static readonly MAX_DISPLAY_COUNT = 4;
  static readonly DEFAULT_LOCALE = "ja-jp";
  static readonly DEFAULT_AXIOS_REQUEST_CONFIG = {
    timeout: 5000,
  };
}
